/**
 * External dependencies
 */
import React from 'react';

const ErrorBanner = ( { children } ) => (
	<div className="wp-block-crowdsignal-forms-poll__error-banner">
		{ children }
	</div>
);

export default ErrorBanner;
