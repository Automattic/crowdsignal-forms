/**
 * External dependencies
 */
import React from 'react';
import PropTypes from 'prop-types';

const Applause = ( { className } ) => (
	<div className={ className }>Applause placeholder</div>
);

Applause.propTypes = {
	className: PropTypes.string,
};

export default Applause;
